# Supabase (projeto de produção por padrão; use .env.staging para apontar
# para o projeto de staging durante o desenvolvimento/testes destrutivos).
VITE_SUPABASE_URL=https://nvkfgczahyxzgoomkavk.supabase.co
VITE_SUPABASE_ANON_KEY=

# Domínio público do app, usado para montar links (confirmação de e-mail,
# onboarding, etc). Sem fallback para produção — se ausente, o app usa
# window.location.origin (a própria origem que está servindo a página).
VITE_APP_URL=https://app.elevareconsultoria.com
